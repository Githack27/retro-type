services:
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: retro-type-webapp
    ports:
      - "10000:10000"
    environment:
      - PORT=10000
      - API_URL=http://host.docker.internal:5000
    extra_hosts:
      - "host.docker.internal:host-gateway"
